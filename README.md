This project solves the Graph Coloring Problem using backtracking.
It reads graph data from a file, assigns up to K colors to N nodes, and checks whether a valid coloring exists.

Features

Backtracking-based graph coloring algorithm

Reads graph input from a text file

Supports any number of nodes, edges, and colors

Detects conflicts automatically

Clean Python implementation
